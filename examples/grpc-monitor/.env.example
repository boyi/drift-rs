# Example environment variables for gRPC Monitor
# Copy this file to .env and fill in your values

# gRPC endpoint (REQUIRED) - must be a gRPC-enabled endpoint like Yellowstone gRPC
# Note: Standard Solana RPC endpoints (api.mainnet-beta.solana.com) do NOT support gRPC
GRPC_URL=http://localhost:10000
# For production, use a proper gRPC endpoint like:
# GRPC_URL=https://your-grpc-provider.com

# gRPC authentication token (optional)
# Only needed if your gRPC endpoint requires authentication
GRPC_X_TOKEN=your-grpc-token-here

# Solana RPC endpoint (optional - has defaults)
# Mainnet default: https://api.mainnet-beta.solana.com
# Devnet default: https://api.devnet.solana.com
RPC_URL=https://api.devnet.solana.com